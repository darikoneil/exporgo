# Logging (`exporgo.log`)

The base logging layer: a parameterized {func}`~exporgo.log.init_logger`, the
{class}`~exporgo.log.LogLevel` scale, and decorators that record calls, arguments, return
values, and timing.

```{eval-rst}
.. automodule:: exporgo.log
   :members:
   :show-inheritance:
```
