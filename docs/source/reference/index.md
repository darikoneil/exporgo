# API Reference

The reference documents exporgo's public surface — the names each layer exports in its
`__all__`. It is generated directly from the source docstrings, so it always matches the
installed version.

```{toctree}
:maxdepth: 2

log
study
datastore
```
