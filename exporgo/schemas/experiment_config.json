{
    "$defs": {
        "Priority": {
            "enum": [
                0,
                1,
                2,
                3,
                4,
                5,
                6
            ],
            "title": "Priority",
            "type": "integer"
        }
    },
    "additionalProperties": false,
    "description": "Recipe for defining an experiment",
    "properties": {
        "key": {
            "default": null,
            "title": "Unique key for the experiment type in the registry",
            "type": "string"
        },
        "supplemental_file_sets": {
            "anyOf": [
                {
                    "type": "string"
                },
                {
                    "items": {
                        "type": "string"
                    },
                    "type": "array"
                },
                {
                    "type": "null"
                }
            ],
            "default": null,
            "title": "List of file sets for organizing experiment"
        },
        "priority": {
            "$ref": "#/$defs/Priority",
            "default": 3,
            "title": "Global priority of the experiment"
        }
    },
    "title": "ExperimentConfig",
    "type": "object"
}