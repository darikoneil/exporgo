name: rust-ci

on:
  push:
    branches: [master, science]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "build.rs"
      - "src/**"
      - "tests/**"
      - "template/**"
      - ".gitattributes"
      - ".github/workflows/rust-ci.yml"
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "build.rs"
      - "src/**"
      - "tests/**"
      - "template/**"
      - ".gitattributes"
      - ".github/workflows/rust-ci.yml"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      # rustfmt.toml uses nightly-only options (brace_style = "AlwaysNextLine"),
      # so formatting is checked on nightly; clippy and tests stay on stable.
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo +nightly fmt --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test
      # The library must stay buildable without the CLI (for a future GUI).
      - run: cargo check --no-default-features
